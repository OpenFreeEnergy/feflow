name: feflow-test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - gufe
  - numpy
  - openfe  # TODO: Remove once we don't depend on openfe
  - openff-units
  - openmm
  - pymbar
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  #  - codecov

