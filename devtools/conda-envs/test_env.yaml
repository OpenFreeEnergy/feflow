name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - openfe
  - perses  # Remove once it only depends on openmmtools/openfe
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

