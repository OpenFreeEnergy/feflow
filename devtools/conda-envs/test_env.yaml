name: feflow-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - gufe >=0.9.5
  - numpy
  - openfe>=1.0.0rc0  # TODO: Remove once we don't depend on openfe
  - openff-units
  - openmm
  - pymbar <4
  - pydantic=1  # TODO: Modify when we support pydantic 2
  - python

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  #  - codecov

