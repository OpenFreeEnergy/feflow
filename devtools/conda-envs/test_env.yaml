name: feflow-test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - openfe  # TODO: Remove once we don't depend on openfe
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  #  - codecov

