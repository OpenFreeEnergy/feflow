name: feflow-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - gufe
  - numpy
  - openfe  # TODO: Remove once we don't depend on openfe
  - openff-units
  - openmm
  - pymbar <4
  - pydantic=1  # TODO: Modify when we support pydantic 2
  - python

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  #  - codecov

