name: feflow-test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - gufe
  - openfe  # TODO: Remove once we don't depend on openfe
  - pip
  - pydantic=1  # TODO: Modify when we support pydantic 2
  - python

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  #  - codecov

